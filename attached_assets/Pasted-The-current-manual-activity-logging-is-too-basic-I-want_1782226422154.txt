The current manual activity logging is too basic. I want to upgrade the app to function like Strava, using the device's GPS to track the user's route, distance, and pace in real-time. 

Please implement live GPS tracking by following these technical requirements:

1. NEW DEPENDENCIES & SETUP:
- Install and configure `expo-location` to access device GPS.
- Install `react-native-maps` to display the user's route visually.
- Install a utility like `geolib` (or write a Haversine formula function) to accurately calculate the distance between GPS coordinates.

2. LIVE TRACKING UI (The "Record" Screen):
- Replace the manual entry form with a live tracking interface.
- The top half of the screen should display a live Map (`react-native-maps`) showing the user's current location and a Polyline drawing the route they have taken. Use a minimalist map styling if possible, matching our pink/purple aesthetic.
- The bottom half should display real-time metrics in large, clean typography: 
  - Time (MM:SS)
  - Distance (km)
  - Current Pace (min/km)
- Include prominent, easily tappable buttons for "Start", "Pause", and "Finish Workout".

3. CORE LOGIC & PERMISSIONS:
- When the user opens the Record screen, gracefully request Foreground Location Permissions. If denied, show a friendly fallback message explaining why GPS is needed.
- When "Start" is tapped, use `Location.watchPositionAsync` to subscribe to location updates (set the accuracy to `Highest` or `Balanced` for running/walking).
- Store the incoming coordinates in a state array.
- Every time a new coordinate comes in, calculate the incremental distance from the last point, add it to the total distance, and update the live pace.

4. SAVING TO SUPABASE:
- When the user taps "Finish", stop the location subscription.
- Take the final calculated Distance, Total Time, and Average Pace, and pass them to the Activity Summary screen.
- Save this finalized data to the Supabase `Activities` table. 

Please confirm you understand these technical requirements and begin by installing the necessary packages and setting up the location permissions and live map view.